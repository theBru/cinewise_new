<% if object.errors.any? %>
  
      <p class="error">Something went wrong. That's ok. We seem to have <%= pluralize(object.errors.count, "error") %></p>.
    
    <ul>
    <% object.errors.full_messages.each do |msg| %>
      <li>* <%= msg %></li>
    <% end %>
    </ul>
  </div>
<% end %>