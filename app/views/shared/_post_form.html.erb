<%= form_for(@post) do |f| %>
  <%= render 'shared/error_messages', object: f.object %>
  
  	<%= f.text_field :title, placeholder: "which film?" %>
    <%= f.text_area :content, placeholder: "write away..." %>
  
  <%= f.submit "Post", class: "postButton" %>
<% end %>